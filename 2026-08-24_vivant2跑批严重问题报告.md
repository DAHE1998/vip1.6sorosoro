# vip1.6sorosoro「跑一轮」严重问题报告（已解决）

- **日期**：2026-08-24
- **服务器**：dahe@192.168.11.11（RTX 4070 12G，驱动 610.43，CUDA 13）
- **项目**：/home/dahe/videotools/vip/vip1.6sorosoro（本地镜像：/Users/dahe/Documents/视频整理企划/vip1.6sorosoro）
- **输入**：input/vivant2（[MagicStar] VIVANT EP01/EP02 [WEBDL] [1080p].mkv，共 4.1G，B 逻辑全局合并）
- **目标**：`bash batch_pipeline.sh` 全流程跑通，验证 sorosoro 自包含环境接线 + 自编译 ffmpeg + vLLM（Qwen3-ASR / Qwen3-VL-4B desc）环节

---

## 一、结论摘要

**`bash batch_pipeline.sh` 已全流程跑通。**

| 视频 | 产物 |
|---|---|
| EP01 `25ca10` | `shikakutensha/25ca10_desc.json`（171KB，546 段 VLM 描述）|
| EP02 `27a403` | `shikakutensha/27a403_desc.json`（117KB）|

日志输出 `===== ... 全部完成 =====`，无 ERROR / Traceback / RuntimeError。仅剩 4 条 cv2 `imread_` 警告（缺个别帧 jpg，不影响主流程）。

---

## 二、历次运行记录

| 轮次 | 日志 | 结果 | 结论 |
|---|---|---|---|
| run1 | /tmp/pipeline_run.log | audio 崩溃 | 缺 `ffmpeg-9.0/ffmpeg` |
| run2 | /tmp/pipeline_run2.log | 用户停止 | 临时软链 ffmpeg 后能抽音轨 |
| run3-5 | /tmp/pipeline_run[345].log | audio 卡死 | vLLM EngineCore 40 线程 futex 死锁 |
| 隔离测试#1 | /tmp/vllm_test.log | 崩溃 | `array.array[int]` 不可下标（flashinfer） |
| 隔离测试#2 | /tmp/vllm_test.log | LOADED_OK 56.6s | flashinfer 补丁 + ninja PATH 后单测通过 |
| run6 | /tmp/pipeline_run6.log | audio 失败 | transcribe.py 返回 1，未暴露真实错误 |
| run7 | /tmp/pipeline_run7.log | audio 通，fuse/desc 失败 | transcribe modality key 错误 + desc bitsandbytes 缺失 + DINO skeleton/npz 不一致 |
| run8 | /tmp/pipeline_run8.log | fuse 仍报 DINO 不一致 | dino skeleton 未包含 fallback key_frames |
| **run9** | **/tmp/pipeline_run9.log** | **全部完成 ✅** | 修复所有 bug 后全流程通过 |

---

## 三、已诊断并修复的问题

### 1. 缺 ffmpeg（audio 抽音轨依赖）
- **修复**：从 `/Users/dahe/Downloads/ffmpeg-9.0.tar.xz` 源码编译 ffmpeg 9.0，放 `$PROJ/ffmpeg/ffmpeg` + `ffprobe`（扁平无嵌套），x264/x265 静态链入
- **接线**：`kikikomu/asr_pipeline.py` 第 25 行改为 `$PROJ/ffmpeg/ffmpeg`

### 2. flashinfer `array.array[int]` 注解崩溃
- **位置**：`env/sorosoro/.../flashinfer/comm/fd_exchange.py:55`
- **修复**：`array.array[int]` → `array.array`（备份 `fd_exchange.py.bak`）

### 3. vLLM 子进程找不到 ninja
- **修复**：`batch_pipeline.sh` 追加 `export PATH="$PROJ_ROOT/env/sorosoro/bin:$PATH"`（备份 `.bak_path`）

### 4. LD_LIBRARY_PATH 组合死锁
- **原因**：env nvidia 库 + `/usr/local/cuda-13.0/lib64` 同时存在导致 CUDA 初始化死锁
- **修复**：LD_LIBRARY_PATH 移除 `/usr/local/cuda-13.0/lib64`（备份 `.bak_ld`）

### 5. OMP/MKL/OPENBLAS/VECLIB/NUMEXPR 线程限制与 vLLM 冲突
- **原因**：`batch_pipeline.sh` 默认设这些变量为 4，vLLM EngineCore 在加载阶段死锁
- **修复**：在 **audio 步骤** 和 **desc 步骤** 前临时 `unset` 这些变量，步骤结束后再恢复（备份 `.bak_omp`）

### 6. Qwen3-ASR multi_modal_data 用了非法 modality key
- **位置**：`kikikomu/transcribe.py:132`
- **现象**：`ValueError: Unsupported modality: kikikomu`
- **修复**：`multi_modal_data={"kikikomu": wav32}` → `{"audio": wav32}`（备份 `transcribe.py.bak_modality`）

### 7. Qwen3-VL-4B 缺 bitsandbytes
- **现象**：`ImportError: Please install bitsandbytes>=0.48.1`
- **修复**：`pip install "bitsandbytes>=0.48.1"` → 安装 0.50.1（在 `sorosoro` 环境内）

### 8. DINO skeleton 与 npz 行数不一致
- **位置**：`shikaku/dino_cluster.py` Mode A 分支
- **原因**：select_frames 中 12 个 shot 无 `key_frames`，`load_select_frames` 用中点 fallback 写入 npz，但保存 skeleton 时仍用原始 shots，导致 skeleton 1548 key_frames vs npz 1560 行
- **修复**：保存 skeleton 前给缺 key_frames 的 shot 补上同样的 fallback（备份 `dino_cluster.py.bak_kfs`）

---

## 四、本次文件改动清单

| 文件 | 改动 | 备份 |
|---|---|---|
| `$PROJ/ffmpeg/`（新增） | ffmpeg + ffprobe 9.0 源码编译产物（扁平） | — |
| `kikikomu/asr_pipeline.py` | FFMPEG_BIN → `ffmpeg/ffmpeg` | 项目 git |
| `kikikomu/transcribe.py` | `multi_modal_data={"audio": wav32}` | `transcribe.py.bak_modality` |
| `shikaku/dino_cluster.py` | skeleton 同步 fallback key_frames | `dino_cluster.py.bak_kfs` |
| `env/sorosoro/.../flashinfer/comm/fd_exchange.py` | `array.array[int]` → `array.array` | `fd_exchange.py.bak` |
| `batch_pipeline.sh` | +`export PATH=...env/sorosoro/bin:$PATH` | `.bak_path` |
| `batch_pipeline.sh` | LD_LIBRARY_PATH 移除 cuda-13.0 | `.bak_ld` |
| `batch_pipeline.sh` | audio/desc 步骤临时 unset OMP 线程变量 | `.bak_omp` |
| `env/sorosoro/` | `pip install bitsandbytes>=0.48.1` | — |

---

## 五、验证结果（run9）

```
[10/11] audio...                  ✅ dialogue.json 生成
[10.5/11] shikakutensha select    ✅ 546 段 / 397 段
[10.6/11] shikakutensha fuse      ✅ 262 张 / 247 张段图
[10.7/11] shikakutensha desc      ✅ 25ca10_desc.json / 27a403_desc.json
===== Mon Aug 24 05:42:55 AM CST 2026 全部完成 =====
```

产物确认：
- `/home/dahe/videotools/vip/vip1.6sorosoro/output/vivant2/shikakutensha/25ca10_desc.json`（171KB）
- `/home/dahe/videotools/vip/vip1.6sorosoro/output/vivant2/shikakutensha/27a403_desc.json`（117KB）

---

## 六、遗留事项

1. **本地镜像未同步**：`/Users/dahe/Documents/视频整理企划/vip1.6sorosoro` 仍是旧快照，不含 `env/`、`ffmpeg/` 及上述脚本改动。如需本地开发/备份，需全量同步（env 约 9.4G）。
2. **4 条 cv2 警告**：`frames/25ca10_f64176.jpg` 等个别帧 jpg 缺失，不影响主流程，属上游抽帧偶发。
3. **服务器 /tmp 清理**：调试脚本和日志可删除（test_omp.sh、run_*.sh、patch_*.py、vllm_test*.log、pipeline_run*.log 等）。
4. **可移植性**：当前 conda 环境 `--copy` 实拷 + ffmpeg 自包含已可整体搬机；跨机到不同绝对路径仍需 `conda-pack` 重定位。

---

*报告更新于 2026-08-24 05:45；所有 bug 已修复并验证通过。*
